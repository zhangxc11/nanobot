# 需求归档 §40–§49

## §40 SubagentManager 单例化 + 跨进程 follow_up 恢复

### 背景

Web worker 模式下，每次请求创建新的 `_create_runner() → AgentLoop → SubagentManager`，turn 结束后被 GC。导致：

- **跨 turn**：同一 session 的后续请求无法找到之前 spawn 的 subagent（`status`/`follow_up`/`stop`/`list` 返回 "Unknown subagent task_id"）
- **跨进程**：worker 进程重启后，即使 subagent session 文件还在磁盘上，也无法通过 task_id 找到对应的 session

Gateway 模式不受影响（AgentLoop 是进程级长生命周期单例）。

### 设计方案

两层解决：进程内单例化 + 跨进程按规则恢复。

#### 子需求 1：AgentLoop 接受外部 SubagentManager

**改动文件**：`nanobot/agent/loop.py`

`AgentLoop.__init__` 新增可选参数 `subagent_manager: SubagentManager | None = None`：
- 传入 → 使用外部实例（web worker 单例模式）
- 不传（默认 None）→ 自己创建（gateway 模式，保持现有行为不变）

```python
class AgentLoop:
    def __init__(
        self,
        ...
        subagent_manager: "SubagentManager | None" = None,  # §40 新增
    ):
        ...
        if subagent_manager is not None:
            self.subagents = subagent_manager
        else:
            self.subagents = SubagentManager(
                provider=provider,
                workspace=workspace,
                bus=bus,
                ...  # 现有参数不变
            )
```

**注意**：`AgentRunner` 也需要透传此参数（如果 AgentRunner 封装了 AgentLoop 构造）。

#### 子需求 2：web-chat worker 维护 SubagentManager 单例

**改动文件**：`web-chat/worker.py`

在模块级维护 SubagentManager 单例，所有 `_create_runner()` 共享：

```python
_subagent_manager: SubagentManager | None = None
_subagent_manager_lock = threading.Lock()

def _get_subagent_manager() -> SubagentManager:
    """获取或创建 SubagentManager 单例。"""
    global _subagent_manager
    if _subagent_manager is not None:
        return _subagent_manager
    with _subagent_manager_lock:
        if _subagent_manager is not None:
            return _subagent_manager
        # 从配置构建（参数与 _create_runner 中 AgentLoop 的一致）
        _subagent_manager = SubagentManager(...)
        return _subagent_manager

def _create_runner():
    agent_loop = AgentLoop(
        ...
        subagent_manager=_get_subagent_manager(),  # §40: 共享单例
    )
```

**注意事项**：
- SubagentManager 持有的 `provider`（ProviderPool）已经是 worker.py 的模块级单例，可直接复用
- `SessionManager` 实例需要共享（或确认无状态可共享）
- `task_keeper` 回调已有模块级 `_keep_subagent_task()` 函数，可直接复用
- `session_messenger` 需要确认是否可作为单例（WorkerSessionMessenger 是无状态 HTTP 调用，可以）

#### 子需求 3：跨进程恢复 — `_check_ownership` 按规则重建 meta

**改动文件**：`nanobot/agent/subagent.py`

当 `_check_ownership()` 在内存 `_task_meta` 中找不到 task_id 时，利用 subagent session 的**确定性命名规则**尝试恢复：

```
task_id = "a1b2c3d4"
parent_session_key = "webchat:1773141981"
→ subagent_session_key = "subagent:webchat_1773141981_a1b2c3d4"
→ session 文件路径 = sessions/subagent_webchat_1773141981_a1b2c3d4.jsonl
```

**不需要扫描磁盘**，直接按规则构造 session key，检查文件是否存在即可（O(1) 文件 stat）。

```python
def _check_ownership(self, parent_session_key: str, target_task_id: str) -> SubagentMeta:
    meta = self._task_meta.get(target_task_id)
    if meta is None:
        # §40: 尝试从磁盘恢复
        meta = self._recover_meta(target_task_id, parent_session_key)
    if meta is None:
        raise ValueError(f"Unknown subagent task_id: {target_task_id}")
    if meta.parent_session_key != parent_session_key:
        raise ValueError(f"Subagent {target_task_id} does not belong to this session")
    return meta

def _recover_meta(self, task_id: str, parent_session_key: str) -> SubagentMeta | None:
    """按命名规则重建 SubagentMeta，用于跨进程 follow_up 恢复。
    
    只恢复 follow_up 所需的最小信息（task_id ↔ session_key 映射），
    不恢复 status/iteration 等运行时状态（进程重启后无意义）。
    """
    if not self.session_manager:
        return None
    
    parent_sanitized = parent_session_key.replace(":", "_")
    subagent_key = f"subagent:{parent_sanitized}_{task_id}"
    
    # 检查 session 文件是否存在
    session_path = self.workspace / "sessions" / f"subagent_{parent_sanitized}_{task_id}.jsonl"
    if not session_path.exists():
        return None
    
    meta = SubagentMeta(
        task_id=task_id,
        subagent_session_key=subagent_key,
        parent_session_key=parent_session_key,
        label="(recovered)",
        origin={"channel": "unknown", "chat_id": "unknown"},
        status="unknown",
        persist=True,
    )
    
    # 缓存到内存
    self._task_meta[task_id] = meta
    self._session_tasks.setdefault(parent_session_key, set()).add(task_id)
    
    return meta
```

#### 子需求 4：`list_subagents` 跨进程增强

`list_subagents()` 需要列出某个 parent session 的所有 subagent。跨进程后内存为空，需要从磁盘补充。

使用 glob 匹配该 parent 前缀的 session 文件（通常只有几个文件，很轻量）：

```python
def list_subagents(self, parent_session_key: str) -> str:
    # §40: 先从磁盘补充
    self._load_disk_subagents(parent_session_key)
    # 然后走原有逻辑...

def _load_disk_subagents(self, parent_session_key: str) -> None:
    """从磁盘 session 文件补充该 parent 的 subagent meta 到内存。"""
    parent_sanitized = parent_session_key.replace(":", "_")
    prefix = f"subagent_{parent_sanitized}_"
    sessions_dir = self.workspace / "sessions"
    if not sessions_dir.exists():
        return
    
    for path in sessions_dir.glob(f"{prefix}*.jsonl"):
        task_id = path.stem.rsplit("_", 1)[-1]
        if task_id not in self._task_meta:
            self._recover_meta(task_id, parent_session_key)
```

### 行为总结

| 场景 | 机制 | 开销 |
|------|------|------|
| 进程内跨 turn（status/follow_up/stop） | SubagentManager 单例，内存中直接查 | O(1) |
| 跨进程 follow_up/status/stop | `_check_ownership` 按规则构造 + 验证文件存在 | O(1) 文件 stat |
| 跨进程 list | glob 匹配该 parent 前缀的 session 文件 | O(n)，n = 该 parent 的 subagent 数 |

### 影响范围

| 文件 | 改动 | 风险 |
|------|------|------|
| `nanobot/agent/subagent.py` | `_check_ownership` 加 fallback；新增 `_recover_meta` + `_load_disk_subagents` | 低 |
| `nanobot/agent/loop.py` | `__init__` 新增 `subagent_manager` 可选参数 | 低 |
| `web-chat/worker.py` | SubagentManager 单例化 | 中 |
| Gateway 模式 | 无改动 | 无 |

### 不做的事

- ❌ 不持久化 status/iteration/last_tool 等运行时信息到磁盘
- ❌ 不改变 session 文件格式或命名规则
- ❌ 不影响 gateway 模式的任何行为

### 测试要点

1. **`_recover_meta` 单元测试**：
   - session 文件存在 → 成功恢复 meta，缓存到内存
   - session 文件不存在 → 返回 None
   - session_manager 为 None → 返回 None
   - 恢复后的 meta 的 parent_session_key 正确
   - 重复调用不重复创建（从缓存返回）

2. **`_load_disk_subagents` 单元测试**：
   - 磁盘有多个 subagent session 文件 → 全部加载
   - 已在内存中的不重复加载
   - sessions 目录不存在 → 不报错

3. **`_check_ownership` 增强测试**：
   - 内存中有 → 直接返回（现有行为不变）
   - 内存中没有，磁盘有 → 恢复并返回
   - 内存中没有，磁盘也没有 → 抛 ValueError

4. **follow_up 跨进程恢复集成测试**：
   - 创建 SubagentManager A → spawn subagent → 记录 task_id
   - 创建 SubagentManager B（模拟进程重启）→ follow_up 同一个 task_id → 成功恢复并发送消息

5. **AgentLoop subagent_manager 参数测试**：
   - 传入外部 SubagentManager → 使用传入的实例
   - 不传 → 自己创建（默认行为）

6. **回归测试**：现有 spawn/follow_up/stop/status 测试全部通过

---

## §41 Usage 日志记录补充 provider name 字段

### 背景

token_usage 表缺少 provider 信息，无法按 provider 统计用量。LiteLLMProvider 构造时接收 provider_name 但未暴露为属性。

### 改动

1. **`nanobot/usage/recorder.py`**：
   - SCHEMA_SQL: token_usage 表新增 `provider TEXT DEFAULT ''`
   - _MIGRATION_SQL: 新增 `ALTER TABLE token_usage ADD COLUMN provider TEXT DEFAULT ''`
   - `record()`: 新增 `provider: str = ""` 参数，INSERT 语句包含 provider 字段

2. **`nanobot/providers/litellm_provider.py`**：
   - `__init__`: 新增 `self.provider_name = provider_name or ""`，暴露为实例属性

3. **`nanobot/agent/loop.py`**：
   - `_run_agent_loop()` 中 `usage_recorder.record()` 调用传入 `provider=getattr(_provider, "provider_name", "")`

4. **`nanobot/agent/subagent.py`**：
   - `_run_subagent()` 中 `usage_recorder.record()` 调用传入 `provider=getattr(self.provider, "provider_name", "")`

### 验收

- 新记录包含 provider 字段
- 旧数据库自动迁移（ALTER TABLE）
- 不传 provider 时默认空字符串

---

## §42 LLM 连接超时优化 — 拆分 connect/read timeout

### 背景

`_LLM_TIMEOUT = 120.0` 对连接超时和读取超时不加区分。连接失败（网络断开）需要等待 120 秒才能触发重试，过于缓慢。

### 改动

**`nanobot/providers/litellm_provider.py`**：
- `import httpx`
- `_LLM_TIMEOUT = httpx.Timeout(connect=30.0, read=120.0, write=30.0, pool=30.0)`
- 连接超时 30s（快速检测网络问题），读取超时 120s（大上下文补全需要时间）

### 验收

- `_LLM_TIMEOUT` 是 `httpx.Timeout` 实例
- connect=30, read=120, write=30, pool=30
- 不破坏现有重试逻辑

---

## §43 修复轮次超限软提醒 — budget alert 改 user role

### 背景

Budget alert 使用 system role，但部分 LLM 不重视 system 消息中的紧急提醒。改为 user role 可提高 LLM 响应率。

### 改动

1. **`nanobot/agent/loop.py`** — budget alert `"role": "system"` → `"role": "user"`
2. **`nanobot/agent/subagent.py`** — 同步修改
3. prompt 内容改为：
   ```
   [System Notice] ⚠️ You have {remaining} tool-call iterations remaining out of {max_iterations}. 
   Prioritize completing your current task. If you cannot finish in time, summarize progress so far and present what you have. Do not acknowledge this notice — continue working.
   ```

### 验收

- budget alert 以 user role 注入
- 不破坏 §32 cache breakpoint 策略
- LLM 能实际看到并响应

---

## §44 Spawn status 异常诊断字段

### 背景

Subagent 遇到 LLM 调用异常时，父 session 无法通过 status 查看异常信息，只能看到 status=running/failed。

### 改动

1. **`nanobot/agent/subagent.py`** — `SubagentMeta` 新增：
   - `error_count: int = 0`
   - `last_error: str | None = None`
   - `last_error_time: str | None = None`

2. **`_chat_with_retry()`** — 新增 `task_id` 参数，LLM 调用异常时更新 meta 的异常字段

3. **`get_status()`** — 返回包含 error_count、last_error、last_error_time

4. **`follow_up()` resume** — 重置 error_count/last_error/last_error_time

### 验收

- subagent 遇到 LLM 异常后，status 返回包含异常信息
- 无异常时为 0/None/None
- resume 时重置

---

## §45 Subagent 返回内容标记隐藏 system prompt

### 背景

Subagent 完成后通过 `_announce_result()` 发送结果通知，其中包含引导 prompt（"Review this result..."）。需要添加标记以便下游消费者识别系统注入部分。

### 改动

**`nanobot/agent/subagent.py`** — `_announce_result()` 中，在 announce_content 开头插入 `<!-- nanobot:system -->` 标记。

### 验收

- 新的 subagent 返回消息中包含 `<!-- nanobot:system -->` 标记
- 标记在 [Subagent Result Notification] 之前
- LLM 行为不受影响（HTML 注释不影响 LLM 解读）

---

## §46 Spawn 并发限制

### 背景

spawn subagent 需要限制并发数量，避免同时启动过多 subagent 导致资源耗尽或 API rate limit。

### 设计要点

1. **并发粒度**：单个父 session 维度（`SubagentManager` 实例级别）。暂不做全局限制。

2. **默认并发数**：4，config 留配置入口（`spawn.max_concurrency`），日常不调整。

3. **排队行为**：
   - 超出并发限制时，`spawn()` 立即返回"已排队"状态（不阻塞 LLM）
   - 返回消息格式：`"Subagent queued (position #N). Current concurrency limit: 4. It will start automatically when a slot becomes available."`
   - 通过 `spawn(status=task_id)` 可查看 `queued` 状态
   - 有空位时自动出队开始执行（FIFO）

4. **SubagentMeta 状态扩展**：
   - 现有 status: `running` / `completed` / `failed` / `max_iterations` / `stopped`
   - 新增: `queued`（排队等待中）
   - SubagentMeta 需保存完整 spawn 参数，以便出队时构建 subagent

5. **排队中任务的 stop**：
   - 支持对 queued 任务执行 stop
   - 直接从队列移除，状态变为 `stopped`
   - 不需要构建子 session

6. **出队触发时机**：
   - 某个 subagent 完成（finished/error/stopped）时检查队列
   - 如果队列非空且有空位，取出队首任务开始执行

7. **与 batch-orchestrator 的关系**：
   - 框架层并发限制是兜底保护
   - batch-orchestrator skill 自身仍然控制节奏（滑动窗口）
   - 两层不冲突

8. **配置读取**：
   - `Config` 新增 `SpawnConfig` with `max_concurrency: int = 4`
   - 从 config 中读取 `spawn.max_concurrency`

### 改动

1. **`nanobot/config/schema.py`** — 新增 `SpawnConfig(Base)` with `max_concurrency: int = 4`；`Config` 新增 `spawn: SpawnConfig`
2. **`nanobot/agent/subagent.py`** — SubagentManager 新增 `_queue`, `_max_concurrency`；`spawn()` 检查并发 → 超限入队；`_on_subagent_done()` 回调出队；`stop_subagent()` 支持 queued 任务
3. **`nanobot/agent/tools/spawn.py`** — 无需改动（路由逻辑不变，排队行为在 SubagentManager 内部处理）
4. **`nanobot/agent/loop.py`** — 传递 `max_concurrency` 参数给 SubagentManager

### 验收

1. 单个父 session 同时运行的 subagent 不超过配置上限（默认 4）
2. 超出时 spawn 返回 queued 状态，不阻塞
3. status 能查到 queued 状态的任务
4. queued 任务支持 stop 取消
5. 有空位时 queued 任务自动出队执行（FIFO）
6. config 中可配置并发上限
7. list 命令显示 queued 任务
8. 不破坏现有 spawn/follow_up/stop/status 功能

---

## §47 SubagentEventCallback 协议

### 背景

web-chat 前端需要实时展示 subagent 的运行状态（进度、重试、完成），
但 nanobot core 没有提供 subagent 生命周期事件的回调机制。

### 设计

定义 `SubagentEventCallback` Protocol，包含 4 个同步回调方法：

1. **`on_subagent_spawned(meta)`** — subagent 创建时（含 queued）
2. **`on_subagent_progress(task_id, iteration, max_iterations, last_tool)`** — 每次迭代开始
3. **`on_subagent_retry(task_id, attempt, max_retries, delay, error, is_fast)`** — LLM 重试前
4. **`on_subagent_done(task_id, status, error)`** — 终态（completed/failed/stopped/max_iterations）

### 三层回调链路

```
nanobot core (SubagentManager)
  ↓ SubagentEventCallback（4 个回调点）
Worker (_subagent_registry + SSE push)
  ↓ SSE 事件 / HTTP API
Frontend（事件驱动 + 按需查询）
```

### 改动

1. **`nanobot/agent/subagent.py`** — 新增 `SubagentEventCallback` Protocol；`SubagentManager.__init__()` 新增 `event_callback` 参数；在 spawn/iteration/retry/done 4 处调用回调
2. **`nanobot/agent/loop.py`** — `AgentLoop.__init__()` 新增 `on_iteration` 回调参数；在主循环每次迭代开始时调用
3. **`tests/test_subagent_event_callback.py`** — 18 项测试覆盖协议定义、4 个回调时机、错误吞没

### 验收

1. `SubagentEventCallback` 是 `@runtime_checkable` Protocol
2. `on_subagent_spawned` 在 spawn（running 和 queued）时被调用
3. `on_subagent_progress` 在每次 iteration 开始时被调用
4. `on_subagent_retry` 在 subagent LLM 重试前被调用
5. `on_subagent_done` 在所有终态（completed/failed/stopped/max_iterations）时被调用
6. 回调中的异常被吞没，不影响 subagent 执行
7. `AgentLoop.on_iteration` 在主循环每次迭代开始时被调用
8. 不传 callback 时行为不变（向后兼容）
9. 全量回归通过

---

## §48 日志增强 + 标记修正 + Budget 优化

### 背景

5 个相关改进需求合并为一个 Phase：
1. SubagentManager 缺少 detail_logger，subagent LLM 调用不记录到 llm-logs
2. llm-logs 和 session JSONL 缺少 provider 信息
3. Subagent 返回内容的 `<!-- nanobot:system -->` 标记位置错误（系统引导在前，final_text 在标记内被截断）
4. 前端隐藏标记需改为闭合标签 `<!-- nanobot:system -->...<!-- /nanobot:system -->`
5. Budget alert prompt 重复代码需抽取公共函数

### 子需求 1：SubagentManager 接入 detail_logger

**改动**:
- `SubagentManager.__init__()` 新增 `detail_logger: LLMDetailLogger | None = None` 参数
- `_run_subagent()` 中 LLM 调用后调用 `detail_logger.log_call()`
- `AgentLoop.__init__()` 中：
  - 创建默认 SubagentManager 时传入 `detail_logger`
  - 使用外部 SubagentManager 时，若其 `detail_logger` 为 None 且自身有 `detail_logger`，则继承

### 子需求 2A：LLM logs 增加 provider 字段

**改动**:
- `LLMDetailLogger.log_call()` 新增 `provider: str | None = None` 参数
- record dict 新增 `"provider"` 字段
- `loop.py` 调用处传入 `provider=getattr(_provider, "provider_name", "")`
- `subagent.py` 调用处传入 `provider=getattr(self.provider, "provider_name", "")`

### 子需求 2B：Session JSONL 增加 provider 字段

**改动**:
- `loop.py` 中 `add_assistant_message` 返回的 messages[-1] 追加 `"provider"` 字段后再 persist
- subagent.py 中 assistant_msg 同样追加 `"provider"` 字段

### 子需求 3+4：Subagent 返回内容标记修正 + 闭合标签

**改动**:
- `_announce_result()` 消息模板改为：final_text 在前，系统引导在 `<!-- nanobot:system -->...<!-- /nanobot:system -->` 闭合标签对内
- final_text 为空时填默认文本 `"(Subagent completed with no output)"`

### 子需求 5：Budget alert 公共函数

**改动**:
- 新建 `nanobot/agent/budget.py`，包含 `build_budget_alert()` 函数
- `loop.py` 和 `subagent.py` 各一行调用替换重复代码
- 函数内部调用 `logger.warning()` 记录 budget alert

### 影响范围

| 文件 | 改动 |
|------|------|
| `nanobot/agent/subagent.py` | detail_logger 参数 + log_call + provider + 标记修正 + budget 调用 |
| `nanobot/agent/loop.py` | detail_logger 继承 + provider 传入 log_call + session provider + budget 调用 |
| `nanobot/usage/detail_logger.py` | log_call 新增 provider 参数 |
| `nanobot/agent/budget.py` | 新文件：build_budget_alert() 公共函数 |

### 验收

1. subagent LLM 调用记录到 llm-logs JSONL
2. llm-logs JSONL 含 provider 字段
3. session JSONL assistant 消息含 provider 字段
4. subagent result 消息：final_text 在 `<!-- nanobot:system -->` 之前
5. 闭合标签格式：`<!-- nanobot:system -->...<!-- /nanobot:system -->`
6. final_text 为空时有默认文本
7. budget alert prompt 含 "Current Turn Budget"
8. `build_budget_alert()` 被 loop.py + subagent.py 调用
9. `logger.warning` 记录 budget alert
10. 全量回归通过
